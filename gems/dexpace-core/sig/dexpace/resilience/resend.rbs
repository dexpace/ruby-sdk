module Dexpace
  module Resilience
    # RETRY-5 through RETRY-8, RECOV-18: the re-sendability gate (R5). Phase 6b widens this
    # module in place with REDIR-6's body-only predicate and its error.
    module Resend
      def self?.eligible?: (Dexpace::Request request) -> bool
    end
  end
end
